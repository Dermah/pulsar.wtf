<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8"/>
    <meta http-equiv="X-UA-Compatible" content="IE=edge"/>
    <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
    <title>Pulsar · Phoenix Framework</title>
    <link rel="stylesheet" href="<%= Routes.static_path(@conn, static_css_for_view(@view_module)) %>"/>
    <link rel="manifest" href="<%= Routes.static_path(@conn, "/manifest.json") %>">
    <!-- Chrome -->
    <meta name="theme-color" content="#222" />
    <!-- iOS Safari -->
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="apple-mobile-web-app-status-bar-style" content="black">
  </head>
  <body>
    <p class="alert alert-info" role="alert"><%= get_flash(@conn, :info) %></p>
    <p class="alert alert-danger" role="alert"><%= get_flash(@conn, :error) %></p>

    <%= render @view_module, @view_template, assigns %>
    <%# <%= static_js_for_view(@view_module)%>
    <script type="text/javascript" src="<%= Routes.static_path(@conn, static_js_for_view(@view_module)) %>"></script>
  </body>
</html>
